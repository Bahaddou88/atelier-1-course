title: Atelier 1
description: HTML/CSS/BOOTSTRAP...
template:
  repo: atelier-1-template
  name: atelier-1
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
